{
  "name": "algomonster-fsrs-extension",
  "version": "1.0.0",
  "description": "FSRS Extension for AlgoMonster",
  "main": "test.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test",
    "build": "rm -rf build && webpack --mode production && cd build && rm -f ../release.zip && zip -r ../release.zip .",
    "dev": "rm -rf build && webpack --mode development --watch"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0",
    "@babel/preset-env": "^7.20.0",
    "@open-spaced-repetition/binding": "^0.5.0",
    "@open-spaced-repetition/binding-wasm32-wasi": "^0.5.0",
    "@playwright/test": "^1.40.0",
    "babel-jest": "^29.0.0",
    "copy-webpack-plugin": "^14.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0",
    "jest-html-reporter": "^3.10.0",
    "ts-fsrs": "^5.4.1",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "author": "",
  "license": "ISC"
}
